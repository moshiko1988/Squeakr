<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
        <li>{{#link-to "users"}}Users{{/link-to}}</li>
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li><a href="#" {{action "signOut"}}><span class="glyphicon glyphicon-log-in"></span> Sign Out</a></li>
        {{else}}
        <li>{{#link-to "sign-up"}}<span class="glyphicon glyphicon-user"></span>Sign Up{{/link-to}}</li>
        <li>{{#link-to "sign-in"}}<span class="glyphicon glyphicon-flash"></span>Sign In{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>


{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="col-md-8 col-md-offset-2">
  {{outlet}}
</div>
